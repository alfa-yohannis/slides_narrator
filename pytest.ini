[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
markers =
    integration: tests that shell out to ffmpeg/poppler (skipped if a tool is missing)
    network: tests that hit the live Edge/Gemini TTS services (opt-in via SLIDES_NARRATOR_NETWORK_TESTS=1)
